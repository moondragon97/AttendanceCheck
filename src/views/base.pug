doctype html
html(lang="ko")
    head
        title #{titleName}
        <link rel="stylesheet" href="https://unpkg.com/mvp.css">
    body
        header
            h1 #{titleName}
            nav
                ul 
                    if !loggedIn
                        li
                            a(href="/join") 회원가입
                        li
                            a(href="/login") 로그인
                    else
                        li
                            a(href="/user/logout") 로그아웃
                    li
                        a(href="/user/attendance") 출석하기
        main
            block content
                    